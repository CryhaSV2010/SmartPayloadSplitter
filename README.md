# Smart Payload Splitter

Инструмент для исследования атак обхода IDS/IPS путём фрагментации пейлоадов и их динамической сборки на стороне сервера.

## 📋 Содержание

1. [Описание](#описание)
2. [Установка](#установка)
3. [Быстрый старт](#быстрый-старт)
4. [Использование клиента](#использование-клиента)
5. [Развертывание с Suricata](#развертывание-с-suricata)
6. [Тестирование обхода IDS](#тестирование-обхода-ids)
7. [Примеры использования](#примеры-использования)
8. [API сервера](#api-сервера)
9. [Устранение неполадок](#устранение-неполадок)
10. [Безопасность и этика](#безопасность-и-этика)

---

## Описание

**Smart Payload Splitter** — это исследовательский инструмент, демонстрирующий технику обхода сигнатурных систем обнаружения вторжений (IDS/IPS) через фрагментацию вредоносных пейлоадов. Пейлоад разбивается на фрагменты случайной длины, которые отправляются в произвольном порядке и собираются на стороне сервера с помощью JavaScript.

### Классификация по MITRE ATT&CK

- **T1027** - Obfuscated Files or Information
- **T1132** - Data Encoding  
- **T1048** - Exfiltration using an alternative protocol

### Архитектура

Проект состоит из трёх основных компонентов:

1. **Клиентская часть** (`client/payload_fragmenter.py`) — Python-скрипт для фрагментации и отправки пейлоада
2. **Серверная часть** (`server/server.py`) — Flask-сервер для приёма фрагментов
3. **Веб-интерфейс** — JavaScript-сборщик фрагментов в браузере

### Принцип работы

1. **Фрагментация**: Пейлоад разбивается на фрагменты случайной длины (в диапазоне min-max)
2. **Перемешивание**: Фрагменты перемешиваются в случайном порядке
3. **Отправка**: Фрагменты отправляются на сервер с задержкой между запросами
4. **Хранение**: Сервер сохраняет фрагменты в памяти, ассоциируя их с session_id
5. **Сборка**: По команде фрагменты собираются в правильном порядке
6. **Выполнение**: Собранный пейлоад готов к выполнению (в демо-версии только отображается)

### ⚠️ ВНИМАНИЕ

**Этот инструмент предназначен исключительно для:**
- Образовательных целей
- Исследований в области информационной безопасности
- Тестирования собственных систем защиты
- Получения разрешения на тестирование (penetration testing)

**Использование этого инструмента для атак на системы без явного разрешения является незаконным и может повлечь уголовную ответственность.**

---

## Установка

### Системные требования

- **Операционная система**: Windows 7/10/11, Linux, macOS
- **Python**: версия 3.8 или выше
- **Пакетный менеджер**: pip
- **Docker** (опционально, для тестирования с Suricata)

### Вариант 1: Локальная установка

#### Установка зависимостей

```bash
pip install -r requirements.txt
```

Или для Linux/Mac:

```bash
pip3 install -r requirements.txt
```

#### Проверка установки

```bash
python -c "import flask, flask_cors, requests; print('Все зависимости установлены успешно!')"
```

### Вариант 2: Docker (рекомендуется для тестирования с Suricata)

#### Установка Docker

**Windows/Mac:**
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
2. Установите и запустите Docker Desktop

**Linux:**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

---

## Быстрый старт

### Запуск сервера (локально)

**Windows:**
```cmd
run_server.bat
```

**Linux/Mac:**
```bash
chmod +x run_server.sh
./run_server.sh
```

**Или вручную:**
```bash
cd server
python server.py
```

Сервер будет доступен по адресу `http://localhost:5000`

### Запуск с Suricata (Docker)

**Windows:**
```cmd
scripts\start_test_environment.bat
```

**Linux/Mac:**
```bash
chmod +x scripts/start_test_environment.sh
./scripts/start_test_environment.sh
```

**Или вручную:**
```bash
docker-compose up -d
```

### Отправка тестового пейлоада

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --assemble
```

### Мониторинг через веб-интерфейс

Откройте в браузере:
- `http://localhost:5000` — главная страница
- `http://localhost:5000/session/<session_id>` — страница для конкретной сессии

---

## Использование клиента

### Базовое использование

```bash
python client/payload_fragmenter.py путь_к_файлу.txt -u http://localhost:5000
```

### Параметры командной строки

#### Обязательные параметры

- `payload` — путь к файлу с пейлоадом (позиционный аргумент)
- `-u, --url` — URL целевого сервера (обязательный параметр)

#### Опциональные параметры

- `-min, --min-size N` — минимальный размер фрагмента в байтах (по умолчанию: 10)
- `-max, --max-size N` — максимальный размер фрагмента в байтах (по умолчанию: 100)
- `-d, --delay F` — задержка между отправками фрагментов в секундах (по умолчанию: 0.1)
- `-r, --report PATH` — путь для сохранения отчёта (по умолчанию: `report_<session_id>.txt`)
- `--no-shuffle` — не перемешивать фрагменты, отправить в исходном порядке
- `--assemble` — автоматически отправить команду на сборку пейлоада после отправки всех фрагментов

### Генерация отчётов

После выполнения клиент автоматически генерирует отчёт, содержащий:

- Общую информацию о сессии
- Статистику отправки фрагментов
- Временные характеристики
- Детальную информацию о каждом фрагменте

Отчёт сохраняется в файл `report_<session_id>.txt` или в указанный файл через параметр `-r`.

---

## Развертывание с Suricata

### Запуск тестового окружения

**Windows:**
```cmd
scripts\start_test_environment.bat
```

**Linux/Mac:**
```bash
chmod +x scripts/start_test_environment.sh
./scripts/start_test_environment.sh
```

### Проверка работы

1. **Проверьте контейнеры:**
```bash
docker-compose ps
```

Должны быть запущены:
- ✅ `smart_payload_server` (порт 5000)
- ✅ `suricata_ids`
- ✅ `suricata_logs_viewer`

2. **Проверьте доступность сервера:**
```bash
curl http://localhost:5000/api/stats
```

3. **Откройте веб-интерфейс:**
```
http://localhost:5000
```

### Локальная установка Suricata

#### Ubuntu/Debian

```bash
sudo add-apt-repository ppa:oisf/suricata-stable
sudo apt-get update
sudo apt-get install suricata

# Копирование конфигурации
sudo cp suricata/config/suricata.yaml /etc/suricata/suricata.yaml
sudo cp suricata/rules/custom-test.rules /etc/suricata/rules/

# Проверка конфигурации
sudo suricata -T -c /etc/suricata/suricata.yaml
```

#### Запуск Suricata

```bash
# Определите сетевой интерфейс
ip addr show

# Запустите Suricata
sudo suricata -c /etc/suricata/suricata.yaml -i eth0
# или для localhost:
sudo suricata -c /etc/suricata/suricata.yaml -i lo
```

---

## Тестирование обхода IDS

### Способ 1: Отправка пейлоада целиком (должен быть обнаружен)

**Windows (PowerShell):**
```powershell
$payload = [Convert]::ToBase64String([System.IO.File]::ReadAllBytes("test_payload.txt"))
curl -X POST http://localhost:5000/api/fragment -H "Content-Type: application/json" -d "{\"session_id\":\"test_full_$(Get-Random)\",\"fragment_index\":0,\"total_fragments\":1,\"data\":\"$payload\"}"
```

**Linux/Mac:**
```bash
PAYLOAD=$(base64 -w 0 test_payload.txt)
curl -X POST http://localhost:5000/api/fragment -H "Content-Type: application/json" -d "{\"session_id\":\"test_full_$(date +%s)\",\"fragment_index\":0,\"total_fragments\":1,\"data\":\"$PAYLOAD\"}"
```

**Ожидаемый результат:** Suricata должна обнаружить пейлоад и создать несколько алертов.

### Способ 2: Маленькие фрагменты (5-20 байт)

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 5 --max-size 20 -d 0.1 --assemble
```

**Ожидаемый результат:** Пейлоад разбит на много маленьких фрагментов, Suricata НЕ должна обнаружить полный пейлоад.

### Способ 3: Большие фрагменты (100-500 байт)

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 100 --max-size 500 -d 0.2 --assemble
```

### Способ 4: Без перемешивания

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 10 --max-size 50 --no-shuffle -d 0.15 --assemble
```

### Способ 5: Медленная отправка (2 сек задержка)

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 15 --max-size 100 -d 2.0 --assemble
```

### Способ 6: Быстрая отправка (0.01 сек)

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 10 --max-size 50 -d 0.01 --assemble
```

### Способ 7: Комбинированный подход

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 8 --max-size 30 -d 0.5 --assemble
```

### Просмотр результатов в Suricata

#### Просмотр логов в реальном времени

**С Docker:**
```bash
docker logs -f suricata_ids
```

**Локально:**
```bash
tail -f /var/log/suricata/fast.log
```

#### Подсчёт алертов

```bash
# Docker
docker exec suricata_ids grep -c "Test Payload" /var/log/suricata/fast.log

# Локально
sudo grep -c "Test Payload" /var/log/suricata/fast.log
```

#### Очистка логов перед тестом

```bash
# Docker
docker exec suricata_ids sh -c "echo '' > /var/log/suricata/fast.log"

# Локально
sudo sh -c "echo '' > /var/log/suricata/fast.log"
```

### Автоматическое тестирование

**Windows:**
```cmd
scripts\test_ids_evasion.bat
```

**Linux/Mac:**
```bash
chmod +x scripts/test_ids_evasion.sh
./scripts/test_ids_evasion.sh
```

Скрипт автоматически:
1. Отправит пейлоад целиком
2. Отправит фрагментированный пейлоад
3. Сравнит результаты

### Сравнение результатов

**Успешный обход IDS:**
- ✅ Пейлоад целиком: обнаружен (1+ алертов)
- ✅ Фрагментированный: НЕ обнаружен (0 алертов)
- ✅ Пейлоад успешно собран на сервере

**Частичный обход:**
- ⚠️ Пейлоад целиком: обнаружен
- ⚠️ Фрагментированный: частично обнаружен (меньше алертов)

**Неудачный обход:**
- ❌ Оба способа дают одинаковое количество алертов

---

## Примеры использования

### Базовый пример

```bash
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000
```

### Настройка размеров фрагментов

```bash
# Маленькие фрагменты
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 5 --max-size 20

# Большие фрагменты
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 --min-size 100 --max-size 500
```

### Настройка задержки

```bash
# Быстрая отправка
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 -d 0.05

# Медленная отправка
python client/payload_fragmenter.py test_payload.txt -u http://localhost:5000 -d 1.0
```

### Комплексный пример

```bash
python client/payload_fragmenter.py test_payload.txt \
    -u http://localhost:5000 \
    --min-size 15 \
    --max-size 150 \
    -d 0.2 \
    --assemble \
    -r comprehensive_report.txt
```

---

## API сервера

### POST /api/fragment

Приём фрагмента от клиента.

**Запрос:**
```json
{
    "session_id": "session_1234567890_1234",
    "fragment_index": 0,
    "total_fragments": 10,
    "data": "base64_encoded_fragment_data"
}
```

**Ответ:**
```json
{
    "status": "success",
    "message": "Фрагмент 0 получен",
    "received_fragments": 1,
    "total_fragments": 10
}
```

### GET /api/fragments/<session_id>

Получение информации о фрагментах для сессии.

**Ответ:**
```json
{
    "status": "success",
    "fragments": {
        "0": true,
        "1": true,
        "2": false
    },
    "total_fragments": 10,
    "received_count": 2
}
```

### POST /api/assemble

Сборка пейлоада из фрагментов.

**Запрос:**
```json
{
    "session_id": "session_1234567890_1234"
}
```

**Ответ:**
```json
{
    "status": "success",
    "message": "Пейлоад успешно собран",
    "payload_size": 1024,
    "payload": "First 1000 characters of assembled payload...",
    "assembled_at": "2024-01-15T10:30:00"
}
```

### GET /api/stats

Получение статистики сервера.

**Ответ:**
```json
{
    "active_sessions": 2,
    "total_fragments_stored": 25,
    "sessions": {
        "session_1234567890_1234": {
            "fragments_count": 10,
            "total_fragments": 10,
            "created_at": "2024-01-15T10:00:00",
            "last_update": "2024-01-15T10:05:00"
        }
    }
}
```

---

## Устранение неполадок

### Ошибка подключения к серверу

**Симптомы:**
```
[-] Ошибка: Connection refused
```

**Решения:**
1. Убедитесь, что сервер запущен
2. Проверьте доступность: `curl http://localhost:5000/api/stats`
3. Проверьте, не блокирует ли файрвол порт 5000

### Ошибка при отправке фрагментов

**Решения:**
1. Проверьте логи сервера
2. Увеличьте задержку: `-d 0.5`
3. Проверьте размер фрагментов

### Фрагменты не собираются

**Решения:**
1. Проверьте отчёт клиента — все ли фрагменты отправлены
2. Проверьте session_id — должен совпадать
3. Используйте параметр `--assemble` или сборку через веб-интерфейс

### Suricata не видит трафик

**Решения:**
1. Убедитесь, что Suricata мониторит правильный интерфейс
2. Проверьте логи: `docker logs suricata_ids`
3. Для localhost используйте интерфейс `lo` (loopback)

### Docker контейнеры не запускаются

**Решения:**
1. Проверьте, что Docker запущен
2. Проверьте доступность портов
3. Посмотрите логи: `docker-compose logs` или `docker compose logs`

### Скрипт start_test_environment.bat не работает

**Проблема:** Скрипт показывает только версию Windows и ничего не делает

**Решения:**

1. **Убедитесь, что вы запускаете скрипт из правильной директории:**
   ```cmd
   cd C:\Users\defuser\Documents\SmartPayloadSplitter
   scripts\start_test_environment.bat
   ```

2. **Проверьте, что Docker Desktop запущен:**
   - Откройте Docker Desktop
   - Дождитесь полной загрузки (иконка в трее должна быть зелёной)

3. **Запустите вручную:**
   ```cmd
   cd C:\Users\defuser\Documents\SmartPayloadSplitter
   docker compose up -d
   ```

4. **Если используете старую версию Docker, попробуйте:**
   ```cmd
   docker-compose up -d
   ```

5. **Проверьте, что docker-compose.yml находится в корне проекта**

### Ошибки установки зависимостей

**Решения:**
```bash
# Используйте правильную версию pip
python -m pip install -r requirements.txt

# Для Linux/Mac
pip3 install -r requirements.txt

# Если проблемы с правами
pip install --user -r requirements.txt
```

---

## Безопасность и этика

### Этичное использование

**Этот инструмент должен использоваться только для:**

1. **Образовательных целей**
   - Изучение техник обхода IDS/IPS
   - Понимание работы систем обнаружения
   - Обучение защите от подобных атак

2. **Исследований**
   - Научные исследования в области информационной безопасности
   - Разработка методов защиты
   - Анализ уязвимостей собственных систем

3. **Тестирования с разрешением**
   - Тестирование собственных систем
   - Тестирование систем с явного письменного разрешения владельца
   - Проведение тестов на проникновение в рамках контракта

### Неэтичное использование

**НЕ используйте этот инструмент для:**

- Атак на системы без разрешения
- Несанкционированного доступа к данным
- Нарушения работы систем
- Любых незаконных действий

### Правовые последствия

Использование инструментов для обхода систем безопасности без разрешения может повлечь:

- **Уголовную ответственность** (статьи 272, 273, 274 УК РФ)
- **Гражданско-правовую ответственность** (возмещение ущерба)
- **Административную ответственность** (штрафы)

### Рекомендации по безопасности

1. **Используйте HTTPS** в production-окружении
2. **Ограничьте доступ** к серверу (файрвол, VPN)
3. **Не храните реальные пейлоады** в репозитории
4. **Регулярно очищайте** старые сессии
5. **Мониторьте** активность на сервере

---

## Структура проекта

```
SmartPayloadSplitter/
├── client/
│   └── payload_fragmenter.py    # Клиентская часть
├── server/
│   └── server.py                 # Серверная часть
├── suricata/
│   ├── config/
│   │   └── suricata.yaml         # Конфигурация Suricata
│   └── rules/
│       └── custom-test.rules     # Правила обнаружения
├── scripts/
│   ├── start_test_environment.*  # Запуск Docker-окружения
│   ├── test_ids_evasion.*        # Автоматическое тестирование
│   └── view_suricata_logs.sh     # Просмотр логов
├── docker-compose.yml            # Конфигурация Docker
├── Dockerfile.server             # Dockerfile для сервера
├── requirements.txt              # Зависимости Python
└── test_payload.txt              # Тестовый пейлоад
```

---

## Известные ограничения

- В демо-версии пейлоад не выполняется, только собирается
- Нет автоматической очистки старых сессий
- Нет поддержки шифрования фрагментов (можно добавить)
- Веб-интерфейс работает только для одной сессии за раз

## Возможные улучшения

- [ ] Добавление шифрования фрагментов
- [ ] Поддержка WebAssembly для сборки
- [ ] Автоматическая очистка старых сессий
- [ ] Поддержка множественных сессий в веб-интерфейсе
- [ ] Интеграция с популярными IDS/IPS для автоматического тестирования
- [ ] Поддержка различных протоколов передачи (не только HTTP)

---

## Литература и ссылки

- [MITRE ATT&CK Framework](https://attack.mitre.org/)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Suricata Documentation](https://suricata.readthedocs.io/)

---

## Лицензия

Этот проект предназначен исключительно для образовательных и исследовательских целей.

---

**Помните: используйте этот инструмент ответственно и только с явного разрешения владельцев тестируемых систем!**
